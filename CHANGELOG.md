# CHANGELOG

画像容量調整ツールの主な変更履歴です。

## Ver.1.3.8

### 圧縮設定

- 通常設定の画質選択順を「最低画質（容量優先） → 標準 → 高画質」に統一
- 印刷設定の画質選択順も同じ順序に統一
- 通常設定・印刷設定とも、起動時のデフォルトを「最低画質（容量優先）」へ変更
- 通常設定の「容量優先」を「最低画質（容量優先）」へ名称変更
- 印刷品質の「下限」を「最低画質（容量優先）」へ名称変更
- 名称変更では圧縮ロジック自体は変更していない

### 詳細設定・表示領域

- 詳細設定を選択していない場合、詳細設定領域を非表示化
- 数値指定と印刷設定も、選択していない側を非表示化
- 画像一覧の表示領域を拡大
- 画像一覧と処理ログの境界をドラッグして表示幅を変更可能にした
- 境界を太くし、操作しやすい表示へ調整

### 変換結果

- JPEG変換後に元画像より容量が増えた場合も保存を継続
- 容量増加時は警告として表示
- 成功＝緑、警告＝黄、失敗＝赤で結果を色分け
- 「削減率」を「容量変化率」へ変更
- 容量削減はマイナス、容量増加はプラスで表示

### 処理ログ

- 画面右側に処理ログを追加
- 最新の「圧縮を開始」の実行を最上部へ表示
- 実行1回ごとに区切り行を追加
- 区切り行に実行日時、設定モード、処理件数、成功 / 警告 / 失敗件数を表示
- 同じ画像を複数回処理してもログを保持し、設定ごとの結果を比較可能にした
- 「すべて削除」は画像一覧のみを消し、ログは保持
- 「ログをクリア」ボタンを追加
- ログ削除前に確認ダイアログを表示
- UIログとCSV出力で同じLogEntryデータを使用
- CSVはUTF-8 BOM付きで保存

処理ログの主な列順：

1. ファイル名
2. 元容量
3. 変換後容量
4. 容量変化率
5. 結果
6. 時刻
7. 入力形式
8. 出力形式
9. 設定
10. JPEG品質
11. 画像サイズ
12. 印刷設定
13. ppi
14. メッセージ

CSV出力項目：

日時、ファイル名、結果、入力形式、出力形式、設定、JPEG品質、元容量、変換後容量、容量変化率、画像サイズ、印刷設定、ppi、メッセージ、入力パス、出力パス

### ファイル追加

- JPEG / PNG / HEIC / HEIFのドラッグ＆ドロップ追加に対応
- 複数ファイルを同時にドロップ可能
- 重複ファイルは追加しない
- 従来の「画像を追加」ボタンも継続

### UI

- 圧縮設定エリアを再設計
- 通常設定は淡いブルー、詳細設定は淡いラベンダーを基調に整理
- 数値指定 / 印刷設定は白背景に整理
- 枠線、角丸、余白を調整
- ボタンをスマホアプリ風のフラットなタップ領域へ変更
- 強い立体表現や操作記号を廃止
- hover / pressed時は控えめな色変化に変更
- 圧縮開始は淡いブルー、削除系は淡い赤、通常操作は白系に整理
- ボタンの高さとフォントサイズの比率を調整
- 通常ボタン：11pt / 高さ34px前後
- 圧縮開始：12pt / 高さ38px前後
- CSV出力 / ログクリア / 参照：10pt / 高さ30px前後

## 開発途中のUI変更について

Ver.1.3.8に至るまで、押しボタンの視認性改善として枠線の強調、立体表現、操作記号の追加なども試行しました。その後、最終的にスマホアプリを意識したフラットUIへ整理しています。

このCHANGELOGでは、READMEに蓄積されていたVer.1.3.8開発中の変更記録を、機能別に整理して掲載しています。
